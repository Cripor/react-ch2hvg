/* import React from 'react';

function Body() {
  return (
    <main>
      <h1>Counter:</h1>
      <div>{state}</div>
      <button onClick={() => setState(i++)}>Click me</button>
    </main>
  );
} */

/* export default Body();
 */
